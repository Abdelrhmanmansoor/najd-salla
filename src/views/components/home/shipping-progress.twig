<div class="najd-shipping" id="shipping-bar" data-threshold="{{ theme.settings.free_shipping_threshold }}">
  <div class="najd-container najd-shipping__inner">
    <p>🚚 أضف <strong id="shipping-remaining">{{ theme.settings.free_shipping_threshold }} {{ store.currency }}</strong> للحصول على شحن مجاني!</p>
    <div class="najd-shipping__bar"><div class="najd-shipping__fill" id="shipping-fill"></div></div>
  </div>
</div>
